include ../../mixins/form-alert.pug
doctype html
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport" content="width=device-width, initial-scale=1")
    title #{pageTitle}
    link(rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css")
    link(rel="stylesheet" href="/styles.css")
  body
    - if (messages.messageSuccess)
        +form-alert-success(messages.messageSuccess)
    - if (messages.messageError)
        +form-alert-error(messages.messageError)
    .container.d-flex.justify-content-center.align-items-center.min-vh-100
      .card.p-4.shadow
        h4.text-center.mb-3 Đăng Nhập
        form(action="/appchat/user/login" method="POST")
          .mb-3
            label.form-label(for="email") Email
            input.form-control(type="email" id="email" name="email" placeholder="Nhập email")
          .mb-3
            label.form-label(for="password") Mật khẩu
            input.form-control(type="password" id="password" name="password" placeholder="Nhập mật khẩu")
          .d-flex.justify-content-between.mb-3
            a(href="#" class="btn btn-link p-0") Quên mật khẩu?
          button.btn.btn-primary.w-100(type="submit") Đăng Nhập
        .text-center.mt-3
          p Chưa có tài khoản?
          a(href="/appchat/user/register" class="btn btn-link") Đăng Ký